/**
 * @NApiVersion 2.1
 */
define(["N/record", "N/search"]
/**
 * @param{record} record
 * @param{search} search
 */, (record, search) => {
  function getManufReturnProdure(options) {
    try {
    } catch (e) {
      log.error("getManufReturnProdure", e.message);
    }
  }

  return { foo, bar };
});
